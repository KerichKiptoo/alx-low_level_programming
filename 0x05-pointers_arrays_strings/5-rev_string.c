#include "main.h"

/**
 * rev_string - reverses a string
 * @s: input string
 */

void rev_string(char *s)
{
	int len, a;

	for (len = 0; len != '\0'; len++)
	{
	}
	for (a = len - 1; a >= 0; a--)
	 {
		 s = a;
	 }
}

