#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 *@int: interger to be checked
 * Return: 0.
 */

int _abs(int)
{
	int i;

	if (i < 0)
	{
		i = i(-1);
		_putchar(i);
	}
	else
		_putchar(i);

	return (0);
}
